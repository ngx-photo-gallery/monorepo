import { ImageContentDirective } from './image-content.directive';

describe('ImageContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageContentDirective();
    expect(directive).toBeTruthy();
  });
});
